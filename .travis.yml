language: haskell
ghc: 7.8
before_install:
  - which cabal
  - cabal install cabal-install
  - which cabal
script:
  - cabal configure --enable-tests
  - cabal build
  - cabal test --show-details=streaming
