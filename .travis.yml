language: haskell
ghc: 7.8
before_install:
  - which cabal
  - cabal install cabal-install
  - which cabal
  - cabal --version
script:
  - echo $PATH
  - which cabal
  - cabal configure --enable-tests
  - cabal build
  - cabal test --show-details=streaming
