language: haskell
ghc: 7.8
before_install:
  - which cabal
  - cabal install cabal-install
  - which cabal
  - cabal --version
script:
  - which cabal
  - cabal configure --enable-tests
  - cabal build
  - cabal test --show-details=streaming
